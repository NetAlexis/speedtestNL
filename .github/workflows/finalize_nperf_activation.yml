name: Finalize nPerf Activation

on:
  push:
    branches:
      - agent/stabilize-speedtest-nperf

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout correction branch
        uses: actions/checkout@v4
        with:
          ref: agent/stabilize-speedtest-nperf
          fetch-depth: 0

      - name: Apply final activation corrections
        run: python scripts/finalize_nperf_activation.py

      - name: Verify and remove temporary files
        run: |
          git diff --check
          rm scripts/finalize_nperf_activation.py
          rm .github/workflows/finalize_nperf_activation.yml
          git diff --check

      - name: Commit corrected implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Finalize nPerf initialization recovery"
          git push origin HEAD:agent/stabilize-speedtest-nperf
